<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zmm.ssm01.mapper.OrderMapper">
  <!--添加购票信息-->
   <insert id="buy" >
      insert into t_order (uid,tid,otime) values (#{uid},#{tid},#{otime})
   </insert>


</mapper>
